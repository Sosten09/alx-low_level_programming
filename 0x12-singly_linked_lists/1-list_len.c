#include <stdlib.h>
#include "lists.h"

/**
 * list_len - Returns the number of elements in a linked list.
 * @h: Nointer to the list_t.
 * Return: Number of elements in h.
 */
size_t list_len(const list_t *h)
{
	 size_t g = 0;

	while (h)

		g++;
	h = h->next;
	return (g);
}

